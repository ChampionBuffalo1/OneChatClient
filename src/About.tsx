import { Helmet } from "react-helmet";

export default function AboutPage() {
    return <div>
        <Helmet>
            <title>
                About OneChat
            </title>
        </Helmet>
        This the about page
    </div>
}